<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <link rel="stylesheet" href="styles.css" />
  <title>Members Only</title>
</head>
<body>
    <%- include('./partials/header.ejs') %>  

    <% if (!user) {%>
    <div class="login-wrapper">
      <div class="login-box">
        <h1>Login</h1>
        <% if (error && error.length > 0) { %>
          <div class="error-message" style="color:red"><%= error[0] %></div>
        <% } %>
        <form action="/login" method="POST">
          <label for="username">Username or Email</label>
          <input type="text" id="username" name="username" required />

          <label for="password">Password</label>
          <input type="password" id="password" name="password" required />

          <button type="submit" class="btn-primary">Log in</button>
        </form>

        <button onclick="location.href='/'" class="btn-secondary">Back to home</button>

        <p class="signup-text">
          Don’t have an account? <a href="/register">Sign up here.</a>
        </p>
      </div>
    </div>
    <% } else {%>
      <div class="login-wrapper">
        <div class="login-box">
          <h1>Welcome!</h1>
          <p>You are already logged in as <%= user.first_name + " " + user.last_name %>.</p> <br>
          <button onclick="location.href='/'" class="btn-primary">Go to Home</button>
        </div>
      <% } %>
</body>
</html>
